# C#工业视觉上位机｜精简背诵版（day02‑day15+拓展）

## day02 var / dynamic / 集合 / Random

### 核心考点

1. **var**：编译期推导类型，**定义必须赋值**，类型固定。
2. **dynamic**：运行时解析，编译不校验；字段写错**运行才报错**，项目尽量少用。
3. 匿名类型 `new {Name=""}`：属性只读，只能用var接收。
4. 数组：固定长度，`.Length`；List可变集合，`.Count`。
5. Dictionary：Key唯一；`dic["key"]`键不存在直接抛异常；安全取值用`TryGetValue`；安全新增`TryAdd`。
6. Random：**不要循环内new**，种子重复生成相同随机数，全局实例一次。

### 面试问答✨

**Q：var 和 dynamic 的区别？**

> var编译阶段确定类型，定义必须赋值，编译强类型检查；dynamic运行时解析，编译不检查。

**Q：Dictionary直接通过索引取不存在key会怎么样？安全怎么做？**

> 运行报错；使用`TryGetValue`安全取值。

**Q：List的Count、数组Length区别？**

> 数组用`.Length`；List集合用`.Count`。

------

## day03｜运算符、if‑switch、三元

### 核心考点

1. ```
   &&
   ```

   短路与：条件false，后面代码

   不执行

   ；

   ```
   ||
   ```

   短路或，条件true后面不执行；

   ```
   & |
   ```

   非短路两边全部执行。

   > 短路典型用途：`if(n2!=0 && 10/n2>1)`防止除零异常。

2. if省略大括号，**仅紧跟第一行属于if内部**。

3. switch case穿透：不写break，多个case共用同一套逻辑；default为兜底。

4. 三元运算符 `条件?真:假`，返回一个值，适合简单二选一。

### 面试问答✨

**Q：&&与&区别，什么是短路运算？**

> &&是短路运算，条件不满足后面代码不执行，可以规避异常；&两边表达式全部执行。

**Q：switch case穿透什么时候用？**

> 多个case需要执行同一套业务逻辑场景。

------

## day04｜循环 while / do‑while / for / foreach

### 核心考点

1. while：先判断后执行，条件不成立可以一次都不跑。
2. do‑while：**先执行一次，再判断；末尾分号不能丢，至少执行1次**。
3. for：适合已知循环次数，按下标遍历。
4. foreach：遍历集合，拿不到下标；**遍历过程不能Add/Remove，抛集合修改异常**。
5. break：跳出**当前一层循环**；continue：跳过本次循环，直接进入下一轮。

### 面试问答✨

**Q：while 和 do‑while区别？**

> while先判断后执行，有可能一次都不执行；do‑while先执行一次再判断，至少执行一次。

**Q：break 和 continue区别？**

> break终止整个循环；continue跳过本次循环，继续下一轮。

**Q：foreach有什么限制？**

> 遍历集合时，不能增删元素，会报运行异常；拿不到索引下标。

------

## day05｜List手写去重、冒泡排序

### 核心考点

1. List手写去重4种思路

- 双重循环删除，删除后`j--`修复下标塌陷；
- LastIndexOf对比前后下标；
- 利用Dictionary key唯一性；
- 新建List，Contains判断不存在才Add。

1. 冒泡排序：两层循环；外层控制轮次，内层相邻元素两两比较交换。
2. 循环删除List元素，下标会向前移动，容易漏删。

### 面试问答✨

**Q：List手写去重有哪几种思路？**

> ①双重循环删除，修复下标；②LastIndexOf；③字典key唯一性；④新建List+Contains判断。

**Q：冒泡排序两层循环各自作用？**

> 外层控制比较轮次；内层负责相邻元素两两比较交换。

------

## day06｜字符串 + 正则表达式

### 核心考点

1. string是**引用类型，但不可变**；所有字符串方法返回新字符串，原字符串不变。
2. `char`单字符，单引号；可以和ASCII互相转换。
3. 判空：

- `IsNullOrEmpty`：判断null、空字符串`""`
- `IsNullOrWhiteSpace`：判断null、空、全空白空格

1. 常用API：Substring(起始,

   字符个数

   )、IndexOf、LastIndexOf、Split、Join、Replace、Trim系列。

   > Split遇到连续分隔符产生空项，搭配`StringSplitOptions.RemoveEmptyEntries`。

2. 正则：

- `IsMatch`校验；`Match`取第一个；`Matches`获取全部匹配；`Replace`替换。
- `^ $`完整匹配；不加只是包含匹配。
- `()`分组；`(?:)`非捕获分组。

### 面试问答✨

**Q：string为什么不可变？会带来什么影响？**

> 字符串对象一旦创建不可修改；所有修改操作产生新字符串；频繁+=拼接性能差，建议StringBuilder。

**Q：IsNullOrEmpty 和 IsNullOrWhiteSpace区别？**

> IsNullOrEmpty只识别null和空字符串；后者还会把全空格判定为空。

**Q：Match 和 Matches区别？**

> Match拿到第一个匹配项；Matches拿到全部匹配结果集合。

> 工业视觉：串口、TCP报文解析大量使用字符串、正则提取SN码、缺陷数据。

------

## day07｜类型转换、时间、JSON、多维&交错数组

### 核心考点

1. 类型转换

- `Parse()`转换失败抛异常；**TryParse()安全解析，返回bool，上位机优先用**。
- `ToString()`null调用报错；`Convert.ToString()`对null安全。
- `.ToArray()`、`.ToList()`集合互转。

1. DateTime

- `DateTime.Now`本地时间；时间戳是UTC时区，北京时间需要`AddHours(8)`校正。
- 两个DateTime相减得到`TimeSpan`时间间隔；`TotalHours / TotalDays`获取总时长。

1. JSON：`System.Text.Json`，Serialize序列化、Deserialize反序列化。
2. 数组

- 规整二维数组`int[,]`：行列固定等长，`GetLength(0)`取行数。
- 交错数组`int[][]`：数组套数组，子数组长度可以不一样，两层下标访问。

### 面试问答✨

**Q：Parse与TryParse区别？上位机为什么优先TryParse？**

> Parse失败直接崩溃；TryParse返回bool，不会抛异常；解析设备报文脏数据，防止程序闪退。

**Q：int[,]二维数组 和 int[][]交错数组区别？**

> int[,]规整多维数组，整块内存，行列长度全部一致；int[][]交错数组，多个独立子数组，子数组长度可以不同。

> 工业视觉：标定矩阵用二维数组；图片日志文件名用DateTime；MES交互使用JSON。

------

## day08｜函数、参数、异常、Debug

### 核心考点

1. 函数：定义不会执行，调用才执行；`return`返回结果同时**直接结束函数**。
2. 参数

- 形参（定义），实参（调用传入）。
- 可选参数（默认值）：必须放在必选参数后面。
- out输出参数：函数内部**必须赋值**，用于输出多个结果。

1. 作用域：`{}`大括号限定；内部可以访问外部变量；外部不能访问大括号内部变量。
2. 异常处理

```
try{风险代码}
catch(具体异常 ex){处理}
finally{必定执行，释放资源}
```

- 多个catch：**具体异常在前，基类Exception放最后**。
- `throw new`手动抛异常，用于参数合法性校验。

1. Debug：F10逐过程；F11逐语句进入方法；F5运行至断点。

### 面试问答✨

**Q：return有哪两个作用？**

> 返回函数结果；立刻结束当前函数，后面代码不再执行。

**Q：try‑catch‑finally各自用途，finally典型场景？**

> try包裹风险代码；catch捕获异常做处理；finally无论是否异常一定执行；用于关闭串口、文件、网络等资源。

**Q：out参数作用？**

> 实现函数输出多个结果，函数内部必须给out变量赋值。

> 工业视觉：串口、文件IO一定要try‑catch，finally释放硬件资源。

------

## day09｜委托、ref/out/params、Lambda、元组、List高级方法、IO

### 核心考点

1. 委托：把方法当做数据传递；Action无返回；Func带返回；`+=`多播委托绑定多个方法。
2. 值类型、引用类型

- 值类型（栈）：int double bool struct enum；赋值拷贝完整副本。
- 引用类型（堆）：栈存地址，堆存真实对象；string是引用类型但是不可变。

1. 参数

- `params`可变参数，**必须写在参数列表最后，只能一个**。

- `ref`：双向；调用前变量必须初始化；修改会影响外部原始变量。

- ```
  out
  ```

  ：单向输出；调用不需要初始化，函数内部必须赋值。

  > Func、Action**不能使用ref/out**。

1. Lambda：匿名委托简写。
2. 元组：轻量级多返回值，替代out；解构`var(a,b)=元组`，`_`忽略不需要项。
3. List高级方法：Find、FindAll、Exists、TrueForAll、RemoveAll、ForEach、ConvertAll。
4. IO：File、Directory；`Path.Combine()`拼接路径，不要手写斜杠。

### 面试问答✨

**Q：ref 和 out区别？**

> ref调用变量必须初始化，双向可读可写；out只负责输出，调用不用初始化，函数内部必须赋值。

**Q：Find 和 FindAll区别？**

> Find找满足条件第一个元素；FindAll返回全部满足条件新List集合。

**Q：为什么推荐Path.Combine？**

> 自动适配操作系统路径分隔符，避免手写斜杠造成路径错误。

> 工业视觉：读写配置文件、检测报告使用IO。

------

## day10｜LINQ、面向对象基础

### 核心考点

1. LINQ：链式查询，**延迟执行，调用ToList()才会立即执行**。 Where过滤、Select投影、OrderBy、ThenBy二级排序、GroupBy分组；FirstOrDefault找不到返回默认值；Any判断存在；All判断全部满足。
2. OOP三大特性：封装、继承、多态。
3. 访问修饰符

- public：全部可访问
- private：仅本类内部（默认）
- protected：本类 + 子类访问
- internal：当前项目内可访问

1. static静态：属于类，使用`类名.成员`调用；静态方法**不能直接访问实例成员**。
2. 实例构造函数：new对象自动执行；手写构造函数，编译器不再提供默认无参构造。

### 面试问答✨

**Q：LINQ什么是延迟执行，如何立即执行？**

> 遍历/调用聚合方法才真正执行查询；调用ToList()立即执行。

**Q：public / private / protected / internal区别？**

> public全部可见；private仅本类；protected本类+子类；internal仅限当前项目项目。

> 工业视觉：LINQ用来过滤、统计缺陷集合。

------

## day12｜回调函数、继承、多态

### 核心考点

1. 回调函数：**函数作为参数传入**；框架固定流程，可变业务交给外部回调；LINQ底层就是回调。
2. 继承：`子类:父类`；抽取公共代码复用；C#类**只支持单继承**。
3. 多态两种形式

- 虚方法重写：父类`virtual`，子类`override`；父类引用可以接收子类对象，执行子类逻辑。
- 方法重载Overload：**同一个类内**，方法名相同，参数列表不同；返回值不参与重载判断。

### 面试问答✨

**Q：回调函数原理？**

> 将函数当做参数传入；框架只负责固定流程，业务逻辑交给外部传入的回调实现。

**Q：重载和重写的区别？**

> 重载发生在同一个类，方法名相同参数不同；重写发生父子类之间，virtual+override改写虚方法。

> 工业视觉：设备基类继承，多态实现不同相机统一调度。

------

## day13｜抽象类、静态类、密封类、接口、this、属性访问器

### 核心考点

1. 抽象类 abstract

- **不能new实例，专门用来被继承**。
- 可以包含普通成员；abstract抽象方法**没有方法体，强制子类override实现**。
- 子类普通类必须实现全部抽象方法；子类也标记abstract可以不实现。

1. 静态类 static class：不能实例，不能继承；全部成员static，用于工具类。

2. 密封类 sealed：禁止被继承；密封方法`sealed override`，阻止后续子类继续重写。

3. 接口 interface：定义能力规范；成员默认public；

   > 类只能继承一个类；**可以实现多个接口**；实现多态，方便替换实现类。

4. this关键字，static内部不能使用 ①区分同名成员与局部变量；②传递当前实例；③构造函数互相调用`this()`；④索引器。

5. 属性访问器 get / set；set中value代表传入赋值；手写后台字段可以在set中做参数校验。

### 面试问答✨

**Q：抽象类和接口区别？**

> 抽象类可以有字段、普通实现代码；接口只定义能力契约；一个类只能继承一个抽象类，可以实现多个接口。

**Q：this有哪4个用途？static方法能否使用this？**

> 区分同名变量；传递当前实例；构造函数串联；实现索引器；static静态方法不能使用this。

**Q：sealed密封方法有什么要求？**

> 必须配合override，不能直接修饰virtual。

> 工业视觉：定义相机采集接口IImageGrabber；设备参数在set访问器做合法性校验。

------

## day14｜命名空间、struct、async‑await、enum、HttpClient、泛型、项目发布

### 核心考点

1. namespace命名空间：解决类名冲突，using导入命名空间。
2. struct结构体：**值类型**；适合轻量小数据（坐标、点、ROI）；可以实现接口，不能继承类。
3. async‑await异步：async标记方法，await等待任务；`Task.WhenAll()`并行等待多个任务。
4. enum枚举：底层int，限制取值，提升代码可读性。
5. HttpClient网络：尽量复用实例，不要循环new，避免套接字耗尽。
6. 泛型`<T>`：类型参数化，编译类型安全，避免装箱拆箱，List/Dictionary都是泛型。
7. 控制台发布：独立部署自带运行库；Release发行版，去除调试信息做优化。

### 面试问答✨

**Q：struct结构体和class类区别？**

> struct值类型，栈存储，赋值拷贝副本；class引用类型，栈存地址，堆存对象；struct不能继承类。

**Q：async/await作用？Task.WhenAll干什么？**

> async标记异步方法；await等待异步任务完成；WhenAll并行等待多个任务全部结束，提升IO耗时操作效率。

**Q：泛型有什么好处？**

> 编译期类型检查；避免装箱拆箱；性能好。

> 工业视觉：MES通信HttpClient；坐标ROI使用struct；设备状态用enum。

------

## day15｜OOP综合实战｜车辆租还系统

### 核心考点

三层架构

1. Model实体层：只做数据载体；主键设置只读，构造函数赋值保护主键。

2. Manager业务层：封装业务逻辑 + JSON读写。

   > JSON标准流程：判断文件存在 → 读取文本 → 反序列化为List实体 → 内存修改 → 序列化 → 写回磁盘。

3. Program交互层：while+switch菜单，只负责输入输出，调用业务，业务和界面解耦。

业务流程

- 租车：校验客户ID存在 → 校验车辆空闲，修改车辆状态 → 新增租还记录写入JSON。
- 还车：校验租还记录存在、未归还；恢复车辆空闲；TimeSpan计算租赁时长，算出租金；回填归还时间、金额写回JSON。

### 面试问答✨✨

**Q：项目三层架构分别职责？**

> Model实体只存储数据；Manager封装业务逻辑与JSON读写；Program只做菜单交互；业务逻辑与界面交互解耦。

**Q：JSON持久化完整流程？**

> 判断文件是否存在 → 读取文本 → 反序列化为List实体对象 → 内存增删改查 → 序列化 → 覆盖写回磁盘。

**Q：为什么自定义实体，不用Dictionary<string,dynamic>？**

> 实体编译期类型检查，字段写错编译报错；dynamic字典运行时才报错，维护麻烦。

**Q：TimeSpan作用？**

> 两个DateTime相减得到时间间隔，TotalHours拿到总小时数用于租金计算。

> 工业视觉：上位机项目同架构；Model存放缺陷、点位实体；Manager封装业务；JSON/SQLite保存配置、检测记录。

------

## 拓展｜装箱拆箱、partial分部类

### 核心考点

1. 装箱拆箱

- 装箱：**值类型 → object**，栈复制副本放到堆，自动完成；修改原值不会改变堆副本。

- 拆箱：

  object → 值类型

  ，必须强制转换；堆内部类型必须完全匹配，否则抛

  ```
  InvalidCastException
  ```

  。

  > 频繁装箱拆箱增加GC压力；优先泛型集合List，避开ArrayList。

1. partial分部类：同一个类拆分多个cs文件；编译自动合并；全部文件都要写partial，命名空间、类名保持一致，禁止重复成员。

### 面试问答✨

**Q：装箱后修改原始值类型变量，object会跟着变吗？为什么？**

> 不会；装箱是复制一份副本放到堆；原始栈变量和堆副本互相独立。

**Q：partial分部类作用，使用场景？**

> 将一个类拆分多个文件，编译合并；WinForm/WPF窗体项目，窗体自动生成代码与业务代码分开放。

------

# 🎯工业视觉上位机高频面试速记（总结）

1. 集合优先泛型`List<T>`，减少装箱拆箱。
2. IO、串口、TCP网络通信，一定要`try‑catch‑finally`，finally释放硬件/文件资源。
3. 设备业务优先抽象类、接口，依靠多态，方便切换不同型号相机。
4. 属性set访问器做参数校验，拦截非法硬件参数。
5. 配置、检测记录用JSON/SQLite持久化。
6. 耗时IO使用async‑await，防止UI卡死。
7. 硬件设备可以使用单例，防止重复打开设备报占用。

> 需要的话，我可以再输出一份**题目版（模拟笔试，带答案）**。